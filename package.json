{
  "name": "runspend",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "biome check .",
    "format": "biome check --write .",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @runspend/db db:generate",
    "db:migrate": "pnpm --filter @runspend/db db:migrate",
    "db:studio": "pnpm --filter @runspend/db db:studio"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.13",
    "turbo": "2.3.3",
    "typescript": "5.7.3"
  }
}
